@using Microsoft.EntityFrameworkCore.Metadata.Internal
@model EventViewModel
<div class="container mb-5">
    <div class="row">
        <div class="col-lg-12 d-flex justify-content-center">
            <img src="@Model.ImgUrl" alt="картинка мероприятия" style="max-width:1000px; max-height:500px;"/>
        </div>
    </div>
    <div class="col-lg-12 mt-2" style="font-size: 25px">
        <p>
            Рады пригласить на мероприятие под названием "@Model.EventsName" котрое пройдёт @Model.EventDate.
        </p>
    </div>
    @if (Model.Class == null)
    {
        <div class="col-lg-12" style="font-size: 25px">
            <p>
                Мероприятие проводится для всех.
            </p>
        </div>
        <div class="col-lg-12" style="font-size: 25px">
            <p>Для того, чтобы участвовать в мероприятии нужно прийти в фойе школы.</p>
        </div>
    }
    else
    {
        <div class="col-lg-12" style="font-size: 25px">
            <p>
                Мероприятие проводится для @Model.Class.ClassName класса.
            </p>
        </div>
        <div class="col-lg-12" style="font-size: 25px">
            <p>Для того, чтобы участвовать нужно прийти в кабинет №@Model.Cabinet.CabinetNumber.</p>
        </div>
    }
    <div class="col-lg-12" style="font-size: 25px">
        <p>Темой мероприятия будет "@Model.Theme.ThemeName".</p>
    </div>
    <div class="col-lg-12" style="font-size: 25px">
        <p>Проводить мероприятие будет @Model.EventManager.</p>
    </div>
    <div class="col-lg-12" style="font-size: 25px">
        <p>@Model.Description</p>
    </div>
    @if (ViewBag.EventResult.Count > 0)
    {
        <div class="row mt-5 mb-3 d-flex justify-content-center">
            <h2>
                Фотографии прошедшего мероприятия :
            </h2>
        </div>
        <div class="row d-flex justify-content-center">
            <div class="col-lg-9 d-flex justify-content-center">
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        @{ int count = 0; }
                        <ol class="carousel-indicators">
                            @for (int i = 0; i < ViewBag.EventResult.Count; i++)
                            {
                                if (i == 0)
                                {
                                    <li data-target="#carouselExampleIndicators" data-slide-to="@i" class="active"></li>
                                }
                                else
                                {
                                    <li data-target="#carouselExampleIndicators" data-slide-to="@i"></li>
                                }
                            }
                        </ol>
                        @foreach (var result in ViewBag.EventResult)
                        {
                            if (count == 0)
                            {
                                <div class="carousel-item active">
                                    <img src="@result.ImageUrl" alt="..." style="height: 450px; width: 850px; object-fit:cover;">
                                    <div class="carousel-caption d-none d-md-block">
                                    </div>
                                </div>
                            }
                            else
                            {
                                <div class="carousel-item">
                                    <img src="@result.ImageUrl" alt="..." style="height: 450px; width: 850px; object-fit:cover;">
                                    <div class="carousel-caption d-none d-md-block">
                                    </div>
                                </div>
                            }
                            count++;
                        }
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        </div>
    }
</div>